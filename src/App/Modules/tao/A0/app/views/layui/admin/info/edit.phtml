<?php


/**
 * @var \App\Modules\tao\Helper\MyMvcHelper $vv
 */

?>
<form class="layui-form" id="form">

    <?php

    echo $vv->layuiForm()->groups([
            $vv->layuiForm()->input('应用名称', 'title', value: $vv->pick('title'), required: true, formItem: false),
            $vv->layuiForm()->input('组名', 'tag', value: $vv->pick('tag'), formItem: false),
    ]);

    echo $vv->layuiForm()->textarea('备注', 'remark', value: $vv->pick('remark'));

    echo $vv->layuiForm()->submit();
    ?>
</form>
<?php
$vv->layui()->addWindowConfig()->footer();
?>
<script>
    admin.form.submitFirst(() => {
        admin.iframe.close(true);
    })
</script>

