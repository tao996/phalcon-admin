<?php

/**
 * @var \Phalcon\Mvc\View\Engine\AbstractEngine $this
 * @var \Phax\Helper\HtmlHelper $vv
 */
$console = $vv->get('console', true); // 默认后台模板
\App\Modules\tao\TaoAppService::layui()->header();

?>
<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="utf-8">
    <title><?= $vv->getHtmlTitle() ?></title>
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--[if lt IE 9]>
    <script src="https://cdn.staticfile.org/html5shiv/r29/html5.min.js"></script>
    <script src="https://cdn.staticfile.org/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    <?php
    $vv->outputHeaders();
    ?>
    <style><?php
        include __DIR__ . '/tao.css'; ?></style>
</head>
<body>
<div class="<?php
echo $console ? 'tao-container' : '' ?>">
    <?php
    // Flash 消息（来自 flashSession 的 PRG 消息优先显示，其次是直接 flash 消息）
    echo $this->flashSession->output();
    echo $this->flash->output();
    echo $this->view->getContent(); ?>
</div>
</body>

<?php
$vv->outputFooters();
\App\Modules\tao\TaoAppService::layui()->footer();
$vv->appendTemplateJs();
?>
<script>
    $('.successMessage, .errorMessage, .warningMessage, .noticeMessage').each(function () {
        var $msg = $(this);
        $msg.css('position', 'relative').append(
            '<span class="flash-close" style="position:absolute;top:50%;right:12px;transform:translateY(-50%);cursor:pointer;font-size:18px;line-height:1;opacity:0.5;">&times;</span>'
        );
    });
    $(document).on('click', '.flash-close', function () {
        $(this).parent().slideUp(200);
    });
</script>

</html>