<?php
/**
 * @var Exception $e
 */
?>
<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <title>系统错误</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: #f5f5f5; color: #333;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            min-height: 100vh; display: flex; align-items: center; justify-content: center;
        }
        .error-box {
            position: absolute;color: black;text-align: left;
            max-width: 720px; width: 90%; margin: 40px auto;
            background: #fff; border-radius: 8px; box-shadow: 0 2px 12px rgba(0,0,0,.1);
            overflow: hidden;
        }
        .error-header {
            background: #e74c3c; color: #fff; padding: 24px 32px;
        }
        .error-header h1 { font-size: 22px; font-weight: 600; }
        .error-header p { margin-top: 6px; opacity: .85; font-size: 14px; line-height: 32px;}
        .error-body { padding: 24px 32px; }
        .error-body .label { font-size: 13px; color: #999; margin-top: 16px; line-height: 32px;}
        .error-body .label:first-child { margin-top: 0; }
        .error-body .value {
            margin-top: 4px; font-size: 14px; line-height: 1.6;
            word-break: break-all;
        }
        .error-body .value.code {
            background: #f8f8f8; border: 1px solid #eee; border-radius: 4px;
            padding: 12px; font-family: "SFMono-Regular", Consolas, monospace;
            font-size: 13px; max-height: 360px; overflow: auto; white-space: pre-wrap;
        }
        .error-footer {
            border-top: 1px solid #eee; padding: 16px 32px;
            text-align: center; font-size: 13px; color: #999;
        }
    </style>
</head>
<body>
<div class="error-box">
    <div class="error-header">
        <h1>500 — 服务器内部错误</h1>
        <p>抱歉，系统遇到了一个意外错误，请稍后再试。</p>
    </div>
    <div class="error-body">
        <?php if (defined('IS_DEBUG') && IS_DEBUG): ?>
            <div class="label">异常类型</div>
            <div class="value"><?= get_class($e) ?></div>

            <div class="label">错误消息</div>
            <div class="value"><?= htmlspecialchars($e->getMessage(), ENT_QUOTES, 'UTF-8') ?></div>

            <div class="label">文件</div>
            <div class="value"><?= htmlspecialchars($e->getFile(), ENT_QUOTES, 'UTF-8') ?> : <?= $e->getLine() ?></div>

            <div class="label">请求 URL</div>
            <div class="value"><?= htmlspecialchars($_SERVER['REQUEST_URI'] ?? '', ENT_QUOTES, 'UTF-8') ?></div>

            <div class="label">调用栈</div>
            <div class="value code"><?= htmlspecialchars($e->getTraceAsString(), ENT_QUOTES, 'UTF-8') ?></div>
        <?php else: ?>
            <div class="value" style="text-align:center;padding:20px 0;color:#666;">
                错误参考编号：<?= date('mdHis') ?><br>
                如持续出现请联系管理员。
            </div>
        <?php endif; ?>
    </div>
    <div class="error-footer">
        <a href="javascript:history.back()" style="color:#1e9fff;text-decoration:none;">← 返回上一页</a>
    </div>
</div>
</body>
</html>
