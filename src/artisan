#!/usr/bin/env php

<?php

const PATH_ROOT = __DIR__ . DIRECTORY_SEPARATOR;
require_once PATH_ROOT . 'Kernel.php';

Kernel::with(PATH_ROOT)
        ->setupDisplayErrors()
        ->createApplication()
        ->runCLI();

